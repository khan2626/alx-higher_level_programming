#!/usr/bin/python3
# 8-multiple_returns.py


def multiple_returns(sentence):
    """returns length and first character of tuple"""
    str_size = len(sentence)
    if len(sentence) > 0:
        return (str_size)
    else:
        sentence[0] += None
    for char in sentence:
        return (sentence.char[0])
        
