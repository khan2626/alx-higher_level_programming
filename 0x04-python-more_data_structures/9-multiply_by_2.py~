#!/usr/bin/python3
# 9-multiply_by_2.py


def multiply_by_2(a_dictionary):
    """it returns a new diction with all values multiplied by 2"""
    new_dict = list(map(lambda key: [key * 2 for key in a_dictionary], a_dictionary.keys()))
    return new_dict
